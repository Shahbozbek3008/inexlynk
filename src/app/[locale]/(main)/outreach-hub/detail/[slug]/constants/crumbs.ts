import { getHref } from "@/lib/utils/get-href"

export const crumbs = [
    {
        href: getHref({
            pathname: "/[locale]/outreach-hub",
        }),
        label: "Outreach Hub",
    },
    {
        label: "Slug",
    },
]
